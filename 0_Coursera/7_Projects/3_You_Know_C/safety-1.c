#include<stdio.h>

//Main function
int main(void)
{
    int a, b;
    b = -8;
    if ((a=b) != 0)
    {
        puts("Value assigned!");
    } else
    {
        puts("Nothing! Value is zero !");
    }
    return 0;
}
