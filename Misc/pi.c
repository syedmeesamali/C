#include<stdio.h>
#define PI 3.142
/* program body*/
int main(void)
{
	int radius = 5;
	float area;
	printf("Radius of cricle = %d\n", radius);
    area = PI * radius * radius;
    printf("Area will be %f\n", area);
    return 0;
}
	
