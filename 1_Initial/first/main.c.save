#include <stdio.h>
void go_south_east(int lat, int lon)
{
    lat = lat - 1;
    lon = lon + 1;
}

int main()
{
    int latitude = 30;
    int longitude = 45;
    
    while (x > 1)
    {
        printf("X value is: %i\n", x);
        printf("X loc is: %i\n", &x);
        x = x - 1;
    }
    return 0;
}
