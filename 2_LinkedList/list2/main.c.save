#include <stdio.h>
#include <stdlib.h>

typedef struct node
{
    int value;
    struct node *next;
} node;

node * createList(int n);

int main()
{
    int n = 0;
    node * head = NULL;
    printf("\nHow many nodes? ");
    scanf("%d", &n);
    return 0;
}

node * createList(int n)
{
    int i = 0;
    node * head = 
}